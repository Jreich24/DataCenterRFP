export interface News {
    id: string;
    title: string;
    link: string;
    publishedAt: Date;
}

export interface RFP {
    id: string;
    title: string;
    description: string;
    link: string;
    dueDate: Date;
}