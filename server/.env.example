# Cost Control Settings
WEEKLY_BUDGET_USD=5.00
PERPLEXITY_COST_PER_QUERY=0.0002  # $0.0002 per query for PPLX-7B-Online
MAX_DAILY_BUDGET_USD=1.00          # $5 weekly spread across 5 weekdays
MAX_QUERIES_PER_MINUTE=30
MAX_QUERIES_PER_HOUR=300
MAX_QUERIES_PER_DAY=5000           # Adjusts based on daily budget and cost per query

# Rate Limiting
PERPLEXITY_RATE_LIMIT_MS=2000      # One request every 2 seconds
SCRAPING_RATE_LIMIT_MS=60000       # One request per minute

# Caching
CACHE_TTL_MINUTES=60               # Cache responses for 1 hour
NEWS_CACHE_TTL_HOURS=4             # Cache news items for 4 hours
RFP_CACHE_TTL_HOURS=24            # Cache RFPs for 24 hours

# Scheduling
SCRAPE_SCHEDULE="0 9,12,15,17 * * 1-5"  # Run at 9am, 12pm, 3pm, and 5pm on weekdays only